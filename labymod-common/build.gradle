plugins {
    id 'java'
    id 'com.github.johnrengelman.shadow' version '6.1.0'
}

tasks.build.dependsOn('shadowJar')

dependencies {
    compile project(':common')

    compileOnly files("${rootDir}/labymod-1.8.9/libs/lm_api_mc1.8.9.jar")
}

shadowJar {
    archiveName('lm-common.jar')
}